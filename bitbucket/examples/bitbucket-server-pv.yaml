kind: PersistentVolume
apiVersion: v1
metadata:
  name: bitbucket-server-pv
  labels:
    product: atlassian-bitbucket
    type: server
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/tmp/bitbucket-server-pv"
